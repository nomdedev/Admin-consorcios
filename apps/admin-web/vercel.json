{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rootDirectory": "../..",
  "installCommand": "corepack enable pnpm && pnpm install",
  "buildCommand": "pnpm run build --filter=admin-web",
  "outputDirectory": "apps/admin-web/.next",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production",
    "PNPM_FLAGS": "--no-frozen-lockfile"
  }
}